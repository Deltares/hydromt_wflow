name: hydromt-wflow

channels:
  - conda-forge

dependencies:
  - cartopy # to run examples
  - descartes # to run examples
  - hydromt_wflow>=0.3.0
  - jupyter # to run examples
  - matplotlib # to run examples
  # - pcraster # optional
  - pip  # required for pip-installed deps below
  - python=3.10 # fixing python version for binder
  - pip:
    - hydroengine # optional
    - gwwapi      # optional

