setup_lulcmaps:
  lulc_fn : corine     # source for lulc maps: {globcover, vito, corine}
  lulc_mapping_fn: corine_mapping_default  # default mapping for lulc classes
  output_names_suffix: corine # add a suffix to the output map names in staticmaps rather than overwritting existing landuse maps
