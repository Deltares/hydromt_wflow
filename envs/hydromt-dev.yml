name: hydromt-wflow

channels:
  - conda-forge

# note that these are the developer dependencies
dependencies:
  - black
  - hydromt
  - pytest # tests
  - pytest-cov # tests
  - pytest-benchmark # tests
  - toml
  - pip:
    - hydroengine
